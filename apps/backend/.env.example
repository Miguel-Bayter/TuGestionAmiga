# .env.example (plantilla)
# Copiar este archivo a .env.local y ajustar los valores

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=tu_gestion_amiga_db

# Alternative: Full DATABASE_URL (for Prisma)
DATABASE_URL="mysql://user:password@localhost:3306/database_name"

# Server
PORT=3000
NODE_ENV=development

# CORS (para Vite frontend)
CORS_ORIGIN=http://localhost:5173

# Admin user password (opcional - default: admin123)
# ADMIN_PASSWORD=mi_contraseña_segura
